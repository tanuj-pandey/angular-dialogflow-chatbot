name: Deploy

on:
  push:
    branches:
      - main  # Change this to your main branch name

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      
      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 14  # Change this to your desired Node.js version
      
      - name: Install dependencies
        run: npm install
        
      - name: Code Linting
        run: npm run lint

      - name: Unit Testing
        run: npm run test
      
      - name: Build Angular app
        run: npm run build-prod

# Uncomment below lines with appropriate details to deploy     
#      - name: Deploy
#        uses: anvil-solutions/Fast-FTP-Action@2.1.0
#        with:
#        server: ftp.samkirkland.com
#         port: 2222
#         username: myFtpUserName
#         password: xyz
#         method: sftp
